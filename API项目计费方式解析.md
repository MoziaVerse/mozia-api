## NewAPI 项目计费方式解析

### 一、计费基础概念

根据代码分析，NewAPI 的计费系统基于以下核心常量（定义在 [`model_ratio.go`](setting/ratio_setting/model_ratio.go:13-17)）：

```go
const (
    USD2RMB = 7.3 // 1 USD = 7.3 RMB
    USD     = 500 // $1 = 500倍率单位
    RMB     = USD / USD2RMB  // ≈ 68.49
)

// 注释说明：
// 1 倍率 === $0.002 / 1K tokens
// 1 倍率 === ￥0.014 / 1K tokens
```

**[`QuotaPerUnit`](common/constants.go:22)** = 500,000（即 $1 = 500,000 额度）

---

### 二、计费公式

#### 1. 额度计算公式（核心）

```
消耗额度 = (输入tokens + 输出tokens × 补全倍率) × 模型倍率 × 分组倍率
```

#### 2. 模型倍率计算公式（基于人民币价格）

```
模型倍率 = 输入价格(¥/1K tokens) ÷ 0.014
```

或等价于：
```
模型倍率 = 输入价格(¥/1M tokens) ÷ 14
```

#### 3. 补全倍率计算公式

```
补全倍率 = 输出价格 ÷ 输入价格
```

---

### 三、您的模型倍率设置

#### GLM4.7（输入4¥/M，输出16¥/M）

| 参数 | 计算过程 | 结果 |
|------|---------|------|
| **模型倍率** | 4 ÷ 14 | **≈ 0.286** |
| **补全倍率** | 16 ÷ 4 | **= 4** |

#### GLM5（输入6¥/M，输出22¥/M）

| 参数 | 计算过程 | 结果 |
|------|---------|------|
| **模型倍率** | 6 ÷ 14 | **≈ 0.429** |
| **补全倍率** | 22 ÷ 6 | **≈ 3.67** |

---

### 四、完整计算公式汇总

```
┌─────────────────────────────────────────────────────────────┐
│                    NewAPI 计费公式汇总                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 模型倍率 = 输入价格(¥/M tokens) ÷ 14                      │
│                                                             │
│  2. 补全倍率 = 输出价格(¥/M tokens) ÷ 输入价格(¥/M tokens)      │
│                                                             │
│  3. 消耗额度 = (输入tokens + 输出tokens × 补全倍率)             │
│               × 模型倍率 × 分组倍率                            │
│                                                             │
│  4. 额度转美元 = 额度 ÷ 500,000                                │
│                                                             │
│  5. 额度转人民币 = 额度 ÷ 500,000 × 7.3                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 五、配置示例

在模型倍率设置中添加：

```json
{
  "glm-4.7": 0.286,
  "glm-5": 0.429
}
```

在补全倍率设置中添加：

```json
{
  "glm-4.7": 4,
  "glm-5": 3.67
}
```

---

### 六、验证计算

以 GLM5 为例，假设使用 1M 输入 tokens + 0.5M 输出 tokens：

```
消耗额度 = (1,000,000 + 500,000 × 3.67) × 0.429 × 1(分组倍率)
        = (1,000,000 + 1,835,000) × 0.429
        = 2,835,000 × 0.429
        = 1,216,215 额度

折合人民币 = 1,216,215 ÷ 500,000 × 7.3 ≈ ¥17.76

验证：输入6¥ + 输出11¥ = ¥17✓（近似，因补全倍率3.67是约数）
```


### 七、等价额度计算

人民币转额度 = 人民币 × 500,000 ÷ 7.3
100 × 500,000 ÷ 7.3 = 6,849,315.07 额度

